class Solution {
    public int fib(int n) {
        if(n < 2){
            return n == 1 ? 1 : 0;
        }
        int sum = fib(n - 1) + fib(n - 2);
        return sum;
    }
}

//注意下面拆解的时候不能直接写 fib(n) = fib(n - 1) + fib(n - 2); fib(n) 是一个变量。
